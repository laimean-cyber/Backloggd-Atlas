const fs=require('node:fs'),assert=require('node:assert/strict');
const {chromium}=require('C:/Users/Laimean/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});const page=await browser.newPage();await page.route('http://localhost:4174/**',r=>r.fulfill(r.request().url().includes('/api/')?{json:{favourites:[]}}:{contentType:'text/html',body:fs.readFileSync('dist/index.html','utf8')}));await page.goto('http://localhost:4174');const summary=await page.evaluate(()=>{const r=playtimeEstimate(games);return {cutoff:r.cutoff,total:r.totalHours,count:r.excluded.length}});assert.deepEqual(summary,{cutoff:105,total:3851,count:6});await page.locator('#timeExclusions').click();assert.equal(await page.locator('#breakdownList .game-item').count(),6);assert.match(await page.locator('#breakdownList').innerText(),/Team Fortress 2/);await page.locator('#closeBreakdown').click();assert.equal(await page.locator('#breakdown').isVisible(),false);await browser.close();console.log('Passed: Laime total 3,851 hours, six exclusions, 105-hour cutoff, working exclusions list.');})().catch(e=>{console.error(e);process.exit(1)});
