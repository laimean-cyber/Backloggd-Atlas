const fs=require('node:fs'),assert=require('node:assert/strict');
const {chromium}=require('C:/Users/Laimean/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const browser=await chromium.launch({channel:'msedge',headless:true});try{for(const width of [1440,1024,390,320]){const p=await browser.newPage({viewport:{width,height:900}});await p.route('**/*',r=>r.request().isNavigationRequest()?r.fulfill({contentType:'text/html',body:fs.readFileSync('public/index.html','utf8')}):r.abort());await p.goto('http://localhost:4174');assert.equal(await p.locator('.heading .eyebrow').count(),0);assert.equal(await p.locator('#title').innerText(),"Laime's gaming journey in numbers");assert.equal(await p.locator('.lookup-row .intro').count(),1);assert.equal(await p.locator('#lookup #reset').count(),0);assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await p.screenshot({path:`header-${width}.png`});await p.locator('#reset').click();assert.equal(await p.locator('#title').innerText(),"Laime's gaming journey in numbers");await p.locator('#title').evaluate(el=>el.textContent="AReallyLongBackloggdNickname's gaming journey in numbers");assert(await p.evaluate(()=>document.documentElement.scrollWidth<=innerWidth));await p.close()}}finally{await browser.close()}console.log('Header copy, structure, reset, long names and four responsive widths passed.')})().catch(e=>{console.error(e);process.exit(1)});
